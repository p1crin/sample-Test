# テストグループ編集機能：実装チェックリストと進捗トラッキング
## Implementation Checklist and Progress Tracking

このドキュメントは、実装の進行状況を追跡し、実装漏れを防ぐためのチェックリストです。
実装を進めながら、各セクションのチェックボックスにチェックを入れてください。

---

## 📋 全体進捗トラッキング

### フェーズごとの完了率

- [ ] **フェーズ 1: 基礎理解** (目標: 1-2 日)
  - 進捗: __% 完了

- [ ] **フェーズ 2: バックエンド実装** (目標: 2-3 日)
  - 進捗: __% 完了

- [ ] **フェーズ 3: フロントエンド実装** (目標: 2-3 日)
  - 進捗: __% 完了

- [ ] **フェーズ 4: テストと検証** (目標: 1 日)
  - 進捗: __% 完了

---

## 🎓 フェーズ 1: 基礎理解

### 学習タスク

#### 1.1 既存コードの読み込み
- [ ] `/app/(secure)/testGroup/regist/` の全コンポーネントを読む
- [ ] `/app/api/test-groups/route.ts` の POST メソッドを詳細に読む
- [ ] フローチャートを手書きまたは図として描く
- [ ] わからない部分をリストアップ

**学習ポイント**:
```
- POST (作成) の全体フロー
- フロントエンドからバックエンドへのデータフロー
- バリデーションの実装パターン
- タグの処理方法
```

**チェック**: 以下の質問に答えられるか確認
- [ ] テストグループ作成時、フロントエンドからどのような JSON を送信している？
- [ ] バックエンドでどのような順序で処理が実行される？
- [ ] タグはどのように処理されている？

#### 1.2 テストグループ詳細取得 API の理解
- [ ] `/app/api/test-groups/[groupId]/route.ts` の GET メソッドを読む
- [ ] どのデータを取得しているか確認
- [ ] エラーハンドリングの流れを理解

**チェック**: 以下を理解しているか確認
- [ ] GET リクエストで何を返している？
- [ ] タグ情報はどのような形式で返されている？

#### 1.3 認証・認可の理解
- [ ] `/app/lib/auth.ts` を全部読む
- [ ] `canModifyTestGroup()` 関数の詳細を理解
- [ ] 権限チェックのロジックを理解

**チェック**: 以下を理解しているか確認
- [ ] 編集時に誰が権限を持つ？（creator のみ？管理者？）
- [ ] 権限がない場合はどうなる？
- [ ] 権限チェックはどこで実行される？

#### 1.4 型定義の理解
- [ ] `TestGroup` インターフェースを確認
- [ ] `TestGroupFormData` 型を確認
- [ ] `TestRole` enum を確認
- [ ] Prisma スキーマを確認

**チェック**: 以下を記録
- [ ] テストグループの主要フィールド：__________
- [ ] テストグループと タグ の関連付け方法：__________
- [ ] テストロール（test_role）の値：__________

### 学習完了チェック

学習が十分か確認するための自己テスト：

1. 以下のシナリオで、どのような処理が実行されるか説明できるか？
   - [ ] ユーザーがテストグループ編集ページを開く
   - [ ] ユーザーが OEM フィールドを変更する
   - [ ] ユーザーが「更新」ボタンをクリックする
   - [ ] API がリクエストを受け取る

2. エラーハンドリングについて：
   - [ ] バリデーションエラー時の流れを説明できる
   - [ ] 権限なしエラーの流れを説明できる
   - [ ] サーバーエラーの流れを説明できる

3. テストグループ作成との違いを説明できるか：
   - [ ] HTTP メソッドの違い
   - [ ] タグの形式の違い
   - [ ] 必須フィールドの違い
   - [ ] 権限要件の違い

---

## ⚙️ フェーズ 2: バックエンド実装

### 2.1 API エンドポイントの実装

#### ファイル: `/app/api/test-groups/[groupId]/route.ts`

##### PUT メソッド（編集）の実装

- [ ] **基本構造の実装**
  - [ ] 関数シグネチャを定義: `export async function PUT(req, { params })`
  - [ ] `QueryTimer` をインスタンス化
  - [ ] try-catch ブロックを追加

- [ ] **ステップ 1: 認証確認**
  - [ ] `requireAuth(req)` を呼び出す
  - [ ] エラーハンドリング（401）

- [ ] **ステップ 2: パラメータ取得と検証**
  - [ ] `params.groupId` から groupId を取得
  - [ ] parseInt で数値に変換
  - [ ] NaN チェック

- [ ] **ステップ 3: 権限確認**
  - [ ] `canModifyTestGroup(user, groupId)` を呼び出す
  - [ ] false の場合は 403 を返す
  - [ ] ログ出力（WARN）

- [ ] **ステップ 4: リクエストボディのパース**
  - [ ] `req.json()` で body を取得
  - [ ] 必要なフィールドを分割代入

- [ ] **ステップ 5: 基本バリデーション**
  - [ ] oem と model の必須チェック
  - [ ] 400 エラーレスポンス

- [ ] **ステップ 6: 文字列長バリデーション**
  - [ ] 各フィールドの最大長チェック（255 文字）
  - [ ] 400 エラーレスポンス

- [ ] **ステップ 7: 数値バリデーション**
  - [ ] ng_plan_count の範囲チェック（0-9999）
  - [ ] 400 エラーレスポンス

- [ ] **ステップ 8: 日付バリデーション**
  - [ ] test_startdate と test_enddate の形式チェック
  - [ ] 開始日 <= 終了日 のチェック
  - [ ] 400 エラーレスポンス

- [ ] **ステップ 9: DB 更新（トランザクション）**
  - [ ] `prisma.$transaction()` を使用
  - [ ] `tt_test_groups.update()` で更新
  - [ ] 既存タグを削除: `deleteMany()`
  - [ ] 新しいタグを作成: `createMany()`
  - [ ] 更新されたレコードを返す

- [ ] **ステップ 10: ログ出力**
  - [ ] 成功時: INFO レベル
  - [ ] `logAPIEndpoint()` を呼び出す
  - [ ] 正しいパラメータを設定

- [ ] **ステップ 11: レスポンス返却**
  - [ ] ステータスコード 200 を返す
  - [ ] JSON レスポンス: `{ success: true, data: updated }`

- [ ] **エラーハンドリング**
  - [ ] catch ブロックで例外をキャッチ
  - [ ] 認証エラー（401）と他のエラー（500）を区別
  - [ ] 適切なログ出力
  - [ ] ユーザーフレンドリーなエラーメッセージ

##### テスト（curl）

```bash
# 成功時のテスト
curl -X PUT http://localhost:3000/api/test-groups/1 \
  -H "Content-Type: application/json" \
  -H "Cookie: session=YOUR_SESSION" \
  -d '{"oem": "Updated", "model": "X"}'
```

- [ ] **成功時（200）**
  - [ ] レスポンス: `{ success: true, data: {...} }`
  - [ ] ログに INFO が出力される

- [ ] **バリデーションエラー（400）**
  - [ ] レスポンス: `{ success: false, error: {...} }`
  - [ ] ログに WARN が出力される

- [ ] **権限エラー（403）**
  - [ ] レスポンス: `{ error: "権限がありません" }`
  - [ ] ログに WARN が出力される

- [ ] **認証エラー（401）**
  - [ ] レスポンス: `{ error: "認証が必要です" }`
  - [ ] ログに記録される

#### DELETE メソッド（ソフトデリート）の実装

- [ ] **基本構造の実装**
  - [ ] 関数シグネチャを定義
  - [ ] `QueryTimer` をインスタンス化

- [ ] **認証・権限確認**
  - [ ] `requireAuth(req)` を呼び出す
  - [ ] `canModifyTestGroup(user, groupId)` で権限確認

- [ ] **ソフトデリート処理**
  - [ ] `is_deleted = true` に更新
  - [ ] `updated_by` と `updated_at` を更新

- [ ] **ログ出力とレスポンス**
  - [ ] 成功時: INFO レベル
  - [ ] ステータスコード 200

### 2.2 バリデーションとエラーハンドリングのテスト

- [ ] **エッジケースのテスト**
  - [ ] 空の oem: 400 を返すか？
  - [ ] 256 文字以上の model: 400 を返すか？
  - [ ] 負の ng_plan_count: 400 を返すか？
  - [ ] 開始日 > 終了日: 400 を返すか？
  - [ ] 無効な日付形式: 400 を返すか？

- [ ] **権限テスト**
  - [ ] creator でない通常ユーザーで実行: 403 を返すか？
  - [ ] 管理者で実行: 成功するか？
  - [ ] 認証なしで実行: 401 を返すか？

- [ ] **DB 整合性テスト**
  - [ ] タグが正しく削除・再作成されるか？
  - [ ] 部分更新が正しく機能するか？
  - [ ] トランザクションが失敗した場合のロールバックは？

### 実装完了チェック

バックエンド実装が完了したか確認：

- [ ] PUT メソッドが実装されている
- [ ] DELETE メソッドが実装されている
- [ ] すべてのバリデーションが実装されている
- [ ] トランザクション処理が実装されている
- [ ] 適切なエラーハンドリングが実装されている
- [ ] ログ出力が実装されている
- [ ] curl でテスト可能な状態

---

## 🎨 フェーズ 3: フロントエンド実装

### 3.1 バリデーションスキーマの実装

#### ファイル: `/app/(secure)/testGroup/[groupId]/edit/_components/schemas/testGroup-edit-schema.ts`

- [ ] **Zod スキーマの定義**
  - [ ] `testGroupEditSchema` を定義
  - [ ] 各フィールドの型と検証ルールを設定
  - [ ] 必須フィールド: oem, model
  - [ ] オプショナルフィールド: その他

- [ ] **フィールドごとの検証**
  - [ ] oem: 必須、最大 255 文字
  - [ ] model: 必須、最大 255 文字
  - [ ] event: オプション、最大 255 文字
  - [ ] variation: オプション、最大 255 文字
  - [ ] destination: オプション、最大 255 文字
  - [ ] specs: オプション
  - [ ] test_startdate: オプション、日付形式
  - [ ] test_enddate: オプション、日付形式
  - [ ] ngPlanCount: オプション、0-9999
  - [ ] designerTag: オプション、配列
  - [ ] executerTag: オプション、配列
  - [ ] viewerTag: オプション、配列

- [ ] **クロスフィールド検証**
  - [ ] `refine()` で開始日 <= 終了日をチェック

- [ ] **型定義の出力**
  - [ ] `TestGroupEditFormData` 型を export

### 3.2 Container コンポーネントの実装

#### ファイル: `/app/(secure)/testGroup/[groupId]/edit/_components/TestGroupEditFormContainer.tsx`

- [ ] **コンポーネント基本設定**
  - [ ] `'use client'` ディレクティブを追加
  - [ ] props インターフェースを定義
  - [ ] 状態変数を定義

- [ ] **初期状態の設定**
  - [ ] formData: 初期値をすべて設定
  - [ ] loading: true から開始
  - [ ] tags: 空配列
  - [ ] errors: 空オブジェクト
  - [ ] isSubmitting: false

- [ ] **useEffect: データ取得**
  - [ ] マウント時に詳細データを GET
  - [ ] タグリストも同時に取得
  - [ ] 取得したデータを state に設定
  - [ ] エラー時のハンドリング
  - [ ] ログ出力（DEBUG/INFO）

- [ ] **handleChange: フォーム値の変更**
  - [ ] 該当フィールドのみを更新
  - [ ] エラー状態をクリア
  - [ ] 不変性を保つ

- [ ] **handleSubmit: フォーム送信**
  - [ ] e.preventDefault()
  - [ ] バリデーション実行
  - [ ] バリデーションエラー時: フィールドエラーを表示
  - [ ] タグを tag_id 形式に変換（重要！）
  - [ ] PUT リクエスト実行
  - [ ] レスポンス処理
  - [ ] 成功時: リダイレクト
  - [ ] エラー時: エラーメッセージ表示
  - [ ] ログ出力（INFO/WARN/ERROR）

- [ ] **ローディング状態**
  - [ ] loading 中: Loading コンポーネント表示

- [ ] **モーダル表示**
  - [ ] 成功時: 成功メッセージ
  - [ ] エラー時: エラーメッセージ

### 3.3 フォーム UI コンポーネントの実装

#### ファイル: `/app/(secure)/testGroup/[groupId]/edit/_components/TestGroupEditForm.tsx`

- [ ] **コンポーネント基本設定**
  - [ ] `'use client'` ディレクティブを追加
  - [ ] props インターフェースを定義

- [ ] **フォームフィールドの定義**
  - [ ] formFields 配列を作成
  - [ ] 各フィールドの type, name, value, onChange を設定
  - [ ] オプショナルフィールド: options を設定
  - [ ] 日付フィールド: type='date'

- [ ] **UI レンダリング**
  - [ ] form タグで wrap
  - [ ] FormField コンポーネントで各フィールド表示
  - [ ] error がある場合: 赤色で表示
  - [ ] ボタングループ: 更新/キャンセル

- [ ] **条件付きレンダリング**
  - [ ] エラーがある場合: エラーメッセージ表示
  - [ ] isSubmitting 中: ボタンを disabled

### 3.4 ページコンポーネントの実装

#### ファイル: `/app/(secure)/testGroup/[groupId]/edit/page.tsx`

- [ ] **Server Component の実装**
  - [ ] ファイル先頭に `'use client'` がない（Server Component）
  - [ ] メタデータを設定
  - [ ] Suspense で wrap
  - [ ] TestGroupEditFormContainer を表示

- [ ] **エラーバウンダリーの検討**
  - [ ] 必要に応じて error.tsx を作成

### 3.5 コンポーネント間のデータフロー検証

フロントエンドの全体フローを確認：

```
page.tsx
  └─ Suspense
      └─ TestGroupEditFormContainer
          ├─ GET /api/test-groups/{id}
          ├─ GET /api/tags
          ├─ state 管理
          └─ TestGroupEditForm
              ├─ FormField x 12
              └─ handleSubmit
                  └─ PUT /api/test-groups/{id}
```

- [ ] **データフロー確認**
  - [ ] データを正しく取得できるか
  - [ ] フォーム値が正しく更新されるか
  - [ ] 送信されるデータは正しいか
  - [ ] エラーメッセージが正しく表示されるか

### フロントエンド実装完了チェック

- [ ] バリデーションスキーマが実装されている
- [ ] Container コンポーネントが実装されている
- [ ] UI コンポーネントが実装されている
- [ ] ページコンポーネントが実装されている
- [ ] データフローが正しく実装されている
- [ ] エラーハンドリングが実装されている
- [ ] ログ出力が実装されている

---

## 🧪 フェーズ 4: テストと検証

### 4.1 単体テスト

#### API エンドポイントのテスト

- [ ] **正常系テスト**
  - [ ] すべてのフィールドを更新
    - [ ] 入力: 有効なデータ
    - [ ] 期待: 200, { success: true, data: {...} }

  - [ ] 部分更新
    - [ ] 入力: OEM のみ変更
    - [ ] 期待: 200, OEM のみ更新

  - [ ] タグ更新
    - [ ] 入力: 新しいタグ
    - [ ] 期待: 200, タグが正しく更新

- [ ] **バリデーションエラーテスト**
  - [ ] oem なし: 400
  - [ ] model なし: 400
  - [ ] 長すぎる文字列: 400
  - [ ] 無効な日付形式: 400
  - [ ] 開始日 > 終了日: 400
  - [ ] ng_plan_count が範囲外: 400

- [ ] **認可エラーテスト**
  - [ ] creator でないユーザー: 403
  - [ ] 管理者: 成功（200）
  - [ ] 認証なし: 401

- [ ] **サーバーエラーテスト**
  - [ ] DB 接続失敗をシミュレート: 500

#### フロントエンド UI テスト

- [ ] **フォーム入力テスト**
  - [ ] テキスト入力
  - [ ] 日付入力
  - [ ] 数値入力
  - [ ] マルチセレクト（タグ）

- [ ] **バリデーション表示テスト**
  - [ ] 必須フィールドが空の場合: エラーメッセージ表示
  - [ ] 文字数が多すぎる場合: エラーメッセージ表示
  - [ ] 日付の大小関係が不正な場合: エラーメッセージ表示

- [ ] **送信テスト**
  - [ ] 有効なデータで送信: 成功メッセージ表示
  - [ ] API エラーで送信: エラーメッセージ表示
  - [ ] ネットワークエラーで送信: エラーメッセージ表示

### 4.2 統合テスト

#### 全体フローテスト

- [ ] **作成→詳細表示→編集→確認**
  1. [ ] テストグループを作成
  2. [ ] 詳細ページで確認
  3. [ ] 編集ページを開く
  4. [ ] データが正しくプリロードされているか確認
  5. [ ] フィールドを編集
  6. [ ] 保存
  7. [ ] 詳細ページでデータが更新されているか確認
  8. [ ] 一覧で更新内容が反映されているか確認

#### タグ更新テスト

- [ ] **タグ追加**
  1. [ ] 編集ページを開く
  2. [ ] タグを追加
  3. [ ] 保存
  4. [ ] DB で タグ関連付けが追加されているか確認

- [ ] **タグ削除**
  1. [ ] 編集ページを開く
  2. [ ] タグを削除
  3. [ ] 保存
  4. [ ] DB で タグ関連付けが削除されているか確認

- [ ] **タグ変更**
  1. [ ] 編集ページを開く
  2. [ ] タグ A をタグ B に変更
  3. [ ] 保存
  4. [ ] DB で正しく変更されているか確認

#### ログ出力テスト

- [ ] **INFO ログ（成功時）**
  - [ ] method: 'PUT'
  - [ ] endpoint: '/api/test-groups/{groupId}'
  - [ ] userId: 正しいユーザーID
  - [ ] statusCode: 200
  - [ ] executionTime: 計測されている
  - [ ] dataSize: 1

- [ ] **WARN ログ（バリデーションエラー時）**
  - [ ] statusCode: 400
  - [ ] error: エラーメッセージ

- [ ] **WARN ログ（権限エラー時）**
  - [ ] statusCode: 403
  - [ ] error: 'Permission denied'

- [ ] **ERROR ログ（サーバーエラー時）**
  - [ ] statusCode: 500
  - [ ] error: エラーメッセージ

### 4.3 手動テスト

#### シナリオ 1: 正常編集

```
1. テストグループ一覧を表示
2. テストグループを選択 → 詳細ページ
3. [編集] ボタンをクリック
4. 複数フィールドを編集
5. [更新] ボタンをクリック
6. ✅ 成功メッセージが表示される
7. ✅ 一覧にリダイレクトされる
8. ✅ 更新内容が反映されている
9. ✅ ログ: INFO レベルの更新ログ
```

- [ ] **実行**:
- [ ] **結果**:
- [ ] **修正が必要**: □ はい □ いいえ

#### シナリオ 2: バリデーションエラー

```
1. 編集ページを開く
2. OEM フィールドをクリアする
3. [更新] ボタンをクリック
4. ✅ エラーメッセージが表示される（「OEM は必須です」）
5. ✅ API リクエストが送信されない
6. ✅ ログに出力されない
```

- [ ] **実行**:
- [ ] **結果**:
- [ ] **修正が必要**: □ はい □ いいえ

#### シナリオ 3: 権限なしエラー

```
1. 別のユーザーで登録したテストグループの編集ページを開く
2. [更新] ボタンをクリック
3. ✅ 403 エラーが返される
4. ✅ エラーメッセージが表示される
5. ✅ ログ: WARN レベルで権限エラー
```

- [ ] **実行**:
- [ ] **結果**:
- [ ] **修正が必要**: □ はい □ いいえ

#### シナリオ 4: タグ更新

```
1. 編集ページを開く
2. デザイナータグを追加
3. [更新] ボタンをクリック
4. ✅ 成功メッセージが表示される
5. ✅ DB で tt_test_group_tags テーブルが更新されている
6. ✅ 詳細ページで新しいタグが表示される
```

- [ ] **実行**:
- [ ] **結果**:
- [ ] **修正が必要**: □ はい □ いいえ

#### シナリオ 5: キャンセル操作

```
1. 編集ページを開く
2. フィールドを変更する
3. [キャンセル] ボタンをクリック
4. ✅ 前のページ（詳細ページ）に戻る
5. ✅ API リクエストが送信されない
```

- [ ] **実行**:
- [ ] **結果**:
- [ ] **修正が必要**: □ はい □ いいえ

### 4.4 パフォーマンステスト

- [ ] **API 応答時間**
  - [ ] 通常時: 500ms 以下
  - [ ] 計測方法: curl で測定

- [ ] **フロントエンドレンダリング時間**
  - [ ] 初回ロード: 2 秒以下
  - [ ] フォーム操作: 100ms 以下

- [ ] **ネットワーク効率**
  - [ ] 不要な API 呼び出しがない
  - [ ] データサイズが適切

### 4.5 セキュリティテスト

- [ ] **XSS 防止**
  - [ ] HTML タグを含む入力: 正しくエスケープされるか
  - [ ] JavaScript コードを含む入力: 実行されないか

- [ ] **CSRF 防止**
  - [ ] Next.js API Routes の CSRF 保護が機能しているか

- [ ] **認可確認**
  - [ ] 権限がないユーザーが編集できない
  - [ ] creator 以外は編集できない

- [ ] **機密情報の漏洩**
  - [ ] エラーメッセージに機密情報が含まれていない
  - [ ] ログに機密情報が保存されていない

### 実装完了チェック

テストすべてが完了したか確認：

- [ ] 単体テスト: すべて成功
- [ ] 統合テスト: すべて成功
- [ ] 手動テスト: すべてのシナリオ完了
- [ ] パフォーマンステスト: 要件を満たしている
- [ ] セキュリティテスト: 問題なし
- [ ] ログ出力: 正しく機能している

---

## 📝 実装メモ

### 実装中に気づいたこと

実装中に気づいたポイント、問題、解決策をここに記録してください：

```
日付: ___________
項目: ___________
内容: ___________
解決策: ___________
```

### 参考にしたコード

```
ファイル: ___________
行番号: ___________
参考にした理由: ___________
```

---

## 🐛 トラブルシューティング

実装中に問題が発生した場合、ここで対処法を検索してください。
詳細は `LEARNING_GUIDE_TEST_GROUP_EDIT.md` の「トラブルシューティング」セクションを参照。

### よくあるエラー

| エラー | 原因 | 対処法 | 参照 |
|--------|------|--------|------|
| PrismaClientConstructorValidationError | Prisma 初期化エラー | `npx prisma generate` | ガイド §トラブル §1 |
| Cannot read property 'id' of undefined | タグが見つからない | タグ名を確認、大文字小文字 | ガイド §トラブル §2 |
| 401 Unauthorized | 認証失敗 | セッション確認 | ガイド §トラブル §3 |
| 403 Permission denied | 権限不足 | creator か管理者か確認 | ガイド §トラブル §4 |
| 400 Bad Request | バリデーション失敗 | フィールドを確認 | ガイド §トラブル §5 |

---

## ✅ 最終チェックリスト

すべてが完了したか最終確認：

### コード品質
- [ ] TypeScript エラーがない
- [ ] ESLint エラーがない
- [ ] コード フォーマット（Prettier）が適用されている
- [ ] 不要なコメント・debugログがない

### 機能
- [ ] 編集機能が完全に動作する
- [ ] タグ更新が正しく機能する
- [ ] エラーハンドリングが適切
- [ ] ログが正しく出力される

### ドキュメント
- [ ] コード内にコメントがある（必要箇所）
- [ ] 実装メモを記録した
- [ ] トラブルシューティングを記録した

### テスト
- [ ] 単体テスト: 全て成功
- [ ] 統合テスト: 全て成功
- [ ] 手動テスト: 全て成功
- [ ] セキュリティチェック: OK

### デプロイ準備
- [ ] 本番環境での動作確認
- [ ] 環境変数の確認
- [ ] ログレベルの確認
- [ ] パフォーマンス要件を満たしている

---

## 🎓 学習完了後のレビュー

実装完了後に確認する項目：

### 理解度確認

以下の質問に答えられるか確認してください：

1. **全体フロー**
   - [ ] テストグループ編集の全体フローを説明できる
   - [ ] 作成と編集の主な違いを説明できる
   - [ ] 権限チェックのロジックを説明できる

2. **バックエンド**
   - [ ] なぜ Prisma トランザクションを使用しているか説明できる
   - [ ] バリデーションのレイヤー（API側）を説明できる
   - [ ] なぜタグの形式が異なるのか説明できる

3. **フロントエンド**
   - [ ] useEffect でデータ取得する理由を説明できる
   - [ ] なぜバリデーションがクライアント側でも必要か説明できる
   - [ ] ログ出力の使い分けを説明できる

4. **セキュリティ**
   - [ ] 認可チェックがなぜ必要か説明できる
   - [ ] XSS 防止について説明できる
   - [ ] なぜ creator のみが編集できるのか説明できる

### 実装パターンの習得

以下のパターンを他のエンドポイント実装に応用できるか確認：

- [ ] **API エンドポイント開発パターン**
  - [ ] 認証・認可チェック
  - [ ] バリデーション
  - [ ] トランザクション処理
  - [ ] ロギング
  - [ ] エラーハンドリング

- [ ] **フロントエンド開発パターン**
  - [ ] データ取得と状態管理
  - [ ] バリデーションスキーマ
  - [ ] フォーム操作
  - [ ] エラー表示

---

**このチェックリストをすべてチェックできたら、テストグループ編集機能の実装は完了です！**

次のステップについては、`LEARNING_GUIDE_TEST_GROUP_EDIT.md` の「次のステップ」セクションを参照してください。

---

**最終更新日**: 2025-12-07
**ドキュメント版**: 1.0
